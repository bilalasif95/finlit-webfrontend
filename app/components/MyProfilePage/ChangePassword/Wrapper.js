import styled from 'styled-components';

const Wrapper = styled.div`
  .basic_info {
    height: 100%;
    .form-group {
      margin: 0 0 2rem;
    }
    .form_footer {
      // position: absolute;
      // bottom: 50px;
      // right: 40px;
    }
  }
`;

export default Wrapper;
