import styled from 'styled-components';

const Wrapper = styled.div`
  margin: 0 auto;
  max-width: 1440px;
  padding: 1.5rem 3rem;
  p {
    color: #9a999a;
    margin: 0 0 1.5rem;
  }
  .form-group {
    margin: 0 0 1.5rem;
  }
  label {
    color: #404041;
  }
  .jodit {
    p {
      color: #404041;
    }
  }
`;

export default Wrapper;
