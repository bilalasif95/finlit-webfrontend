/*
 * My profile Messages
 *
 * This contains all the text for the My profile component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.MyProfile';

export default defineMessages({
  MyProfile: {
    id: `${scope}.MyProfile`,
    defaultMessage: `My Profile`,
  },
});
