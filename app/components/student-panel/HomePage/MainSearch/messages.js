/*
 * Main Search Messages
 *
 * This contains all the text for the Main Search component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.MainSearch';

export default defineMessages({
  HowCanWeHelp: {
    id: `${scope}.HowCanWeHelp`,
    defaultMessage: 'How Can We Help?',
  },
  MainSearchBtn: {
    id: `${scope}.MainSearchBtn`,
    defaultMessage: `Search`,
  },
});
