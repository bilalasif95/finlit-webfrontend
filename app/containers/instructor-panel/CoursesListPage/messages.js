/*
 * Courses List Page Messages
 *
 * This contains all the text for the Courses List Page component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.CoursesListPage';

export default defineMessages({
  CoursesList: {
    id: `${scope}.CoursesList`,
    defaultMessage: 'Courses List',
  },
});
